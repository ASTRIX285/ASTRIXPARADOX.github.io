{
  "name": "forge-destiny-backend",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Cloudflare Worker backend for Forge Destiny authentication and API access.",
  "scripts": {
    "dev": "wrangler dev",
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "check": "npm test && tsc --noEmit && wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260729.1",
    "typescript": "^5.9.0",
    "wrangler": "4.114.0"
  }
}
